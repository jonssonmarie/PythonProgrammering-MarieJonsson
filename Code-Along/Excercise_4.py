# Task
# Compute the slope k and the constant term m of this line 
# using the points A: (4,4) and B: (0,1).

# y = kx + m where k = delta_y/delta_x
# calculate k
delta_y = 1 - 4
delta_x = 0 - 4

k = delta_y/delta_x
# m = 1 

print(f"k = {k}, m = 1 and the equation is y = 0.75x +1 ")
